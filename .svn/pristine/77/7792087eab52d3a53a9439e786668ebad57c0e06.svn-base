package model;

/**
 * Enumération représentant les directions possibles pour certaines actions du jeu.
 * <p>
 * Utilisée notamment pour définir la direction d'un tir (horizontal ou vertical).
 */
public enum Direction {

    /** Direction horizontale. */
    HORIZONTAL,

    /** Direction verticale. */
    VERTICAL
}
